# sherpa-onnx native libs are bundled in the AAR; no additional proguard
# rules are required for v1. If model assets are added to the app module,
# keep them uncompressed (see app/build.gradle.kts noCompress).
