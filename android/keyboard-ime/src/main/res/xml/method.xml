<?xml version="1.0" encoding="utf-8"?>
<!--
  Input method metadata. The subtype list is minimal at M1: English + a
  placeholder Bengali entry so the language toggle in the keyboard maps to a
  real IME subtype. Bengali will grow real key data at M4/M5.
-->
<input-method xmlns:android="http://schemas.android.com/apk/res/android"
    android:supportsSwitchingToNextInputMethod="true">

    <subtype
        android:label="English"
        android:imeSubtypeLocale="en_US"
        android:imeSubtypeMode="keyboard"
        android:isAsciiCapable="true" />

    <subtype
        android:label="বাংলা"
        android:imeSubtypeLocale="bn_BD"
        android:imeSubtypeMode="keyboard"
        android:isAsciiCapable="false" />

</input-method>
